apiVersion: v1
kind: Service
metadata:
  name: {{ include "nomen.name" . }}
  labels:
    {{- include "nomen.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  selector:
    {{- include "nomen.selectorLabels" . | nindent 4 }}
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
